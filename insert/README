插入排序：直接插入排序、二分法和希尔
直接插入排序：
其实总的也是分两步走，第一步查找插入位置，第二步插入位置到插入节点
位置的元素整体向后移动一个位置，然后把插入节点放进插入位置。
二分插入：
二分插入法和直接插入法很类似，只是减少了查找插入点位置的比较次数。实际效率并没有
提高。
希尔排序：
思想：先将待排序元素分组形如L[i,i+d,i+2d,...,i+kd]的特殊子表，分别进行直接插入排序
，当整个表中元素基本有序时，再对全体纪录进行一次直接插入 排序。因为已经基本有序
所以可以很快得到结果.
